[build]
builder = "nixpacks"
buildCommand = "python -m pip install -r trader/requirements.txt"

[deploy]
startCommand = "cd trader && uvicorn api.main:app --host 0.0.0.0 --port 8000"
healthcheckPath = "/api/health"
